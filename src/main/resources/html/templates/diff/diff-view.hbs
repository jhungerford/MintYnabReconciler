<div class="row">
	<div class="small-12 columns text-center">
		<h1>{{currentMonth.humanMonth}}, {{currentMonth.year}}</h1>
	</div>
</div>

<div class="row">
	<div class="small-12 columns">
		<h3>Only in Mint</h3>
	</div>
</div>
{{#each diff in mintOnly}}
	<div {{bind-attr class=":row diff.isCorrect:diff-correct:diff-wrong"}}>
		<div class="small-2 columns">{{formatDay diff.date}}</div>
		<div class="small-3 columns">{{diff.payee}}</div>
		<div class="small-1 columns">{{formatCents diff.cents}}</div>
		<div class="small-6 columns">{{diff.description}}</div>
	</div>
{{else}}
	<div class="row">
		<div class="small-12 columns">
			No transactions for this month.
		</div>
	</div>
{{/each}}

<div class="row">
	<div class="small-12 columns">
		<h3>Only in Ynab</h3>
	</div>
</div>
{{#each diff in ynabOnly}}
	<div {{bind-attr class=":row diff.isCorrect:diff-correct:diff-wrong"}}>
		<div class="small-2 columns">{{formatDay diff.date}}</div>
		<div class="small-3 columns">{{diff.payee}}</div>
		<div class="small-1 columns">{{formatCents diff.cents}}</div>
		<div class="small-6 columns">{{diff.description}}</div>
	</div>
{{else}}
	<div class="row">
		<div class="small-12 columns">
			No transactions for this month.
		</div>
	</div>
{{/each}}

<div class="row">
	<div class="small-12 columns">
		<h3>Incorrect</h3>
	</div>
</div>
{{#each diff in incorrect}}
	<div {{bind-attr class=":row diff.isCorrect:diff-correct:diff-wrong"}}>
		<div class="small-2 columns">{{formatDay diff.date}}</div>
		<div class="small-3 columns">{{diff.payee}}</div>
		<div class="small-1 columns">{{formatCents diff.cents}}</div>
		<div class="small-6 columns">{{diff.description}}</div>
	</div>
{{else}}
	<div class="row">
		<div class="small-12 columns">
			No transactions for this month.
		</div>
	</div>
{{/each}}

<div class="row">
	<div class="small-12 columns">
		<h3>Correct</h3>
	</div>
</div>
{{#each diff in correct}}
	<div {{bind-attr class=":row diff.isCorrect:diff-correct:diff-wrong"}}>
		<div class="small-2 columns">{{formatDay diff.date}}</div>
		<div class="small-3 columns">{{diff.payee}}</div>
		<div class="small-1 columns">{{formatCents diff.cents}}</div>
		<div class="small-6 columns">{{diff.description}}</div>
	</div>
{{else}}
	<div class="row">
		<div class="small-12 columns">
			No transactions for this month.
		</div>
	</div>
{{/each}}

<div class="row diff-paginator">
	<div class="small-12 columns">
		{{date-paginator
			currentMonth=currentMonth
			earliestMonth=controllers.diff.earliestMonth
			latestMonth=controllers.diff.latestMonth
			select="changeMonth"}}
	</div>
</div>
